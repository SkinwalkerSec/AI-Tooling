# Copy to .env and fill in real values. Never commit .env.
# Comma-separated CIDRs/IPs this server is allowed to scan. Leave blank to
# disable the scope guard entirely (only do that for throwaway local labs).
NMAP_ALLOWED_CIDR=10.10.10.0/24
